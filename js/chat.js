var chat = {
	activeId: null,
	activePhoto: null,
	activeName: null,
	init: function(){
		
		
	},
	load: function(id){
		page.activate('chat');
		window.scrollTo(0, document.body.scrollHeight);
	}
};
